spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/manish9742/config-server.git
          clone-on-start: true

server:
  port: 8085